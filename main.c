#include <stdio.h>
#include <stdlib.h>


      // 一个数如果恰好等于不包含它本身所有因子之和，这个数就称为"完数"。
      //例如，6的因子为1、2、3，而6=1+2+3，因此6是"完数"。
      //编程序找出N之内的所有完数，并按下面格式输出其因子
    int f1(int n)
    {
        int i,j;
        for (i=1;i<=n;i++)
           for (j=1;j<i;j++)
           {
               if(i/j==0)


           }
    }






int main()
{

    int n,i;
    printf("")




     return 0;
}
